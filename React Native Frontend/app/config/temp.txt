{
            this.state.showPhotoGallery === true ? (
              <ViewPhotos
               photoArray={this.state.photoArray} />
            ) : (
              <Button
                title={"Select Photo"}
                style={{ width: 200 }}
                onPress={async () => {
                  await this.getPhotosFromGallery();
                  const photos = this.state.photoArray;
                  await console.log('photos: ' + photos);
                  await this.props.navigation.navigate('ViewPhotos', photos);
                }}
              />
            )
          }
